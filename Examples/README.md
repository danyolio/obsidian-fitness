# Examples

These demo workouts are safe, fictional notes for public screenshots and first-run testing.

To preview the dashboards with sample data, copy the files from `Examples/demo-workouts/` into `Workouts/`. Keep real workout notes in `Workouts/`; they are ignored by git by default.
