# Imports

Drop private Apple Health and Garmin export files here.

Everything in this folder is ignored by git except this README and `.gitkeep`, so real health exports and generated import reports stay local.
